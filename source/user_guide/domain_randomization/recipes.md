# DR Recipes

Production-tested DR YAML snippets ready to copy into your owner YAML.

Most production-tested ranges live alongside their task owner YAML under `conf/dr/`. See the per-task DR section in the corresponding {doc}`../../transfer/sim_to_real/overview` page.
