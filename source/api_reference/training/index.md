# `unilab.training` — Training Runtime

Glue between `algos`, `envs` and `ipc`: experiment lifecycle, metric
monitoring, reward bookkeeping, seeding, and the top-level `run` helpers
invoked by the `train` / `eval` / `demo` CLI entrypoints.

```{eval-rst}
.. autosummary::
   :toctree: _autosummary
   :template: autosummary/module.rst
   :recursive:

   unilab.training
```
