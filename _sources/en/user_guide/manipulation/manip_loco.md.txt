# Manip-Loco

The Go2 + Airbot arm task — a combined locomotion + manipulation policy. The robot walks to a target, then the arm performs a manipulation primitive.

See {doc}`../tasks/go2_arm_manip_loco` for the canonical owner config.
